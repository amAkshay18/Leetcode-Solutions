// func countDigitOccurrences(nums []int, digit int) int {
// 	var sb strings.Builder

// 	for _, n := range nums {
// 		sb.WriteString(strconv.Itoa(n))
// 	}

// 	return strings.Count(sb.String(), strconv.Itoa(digit))
// }